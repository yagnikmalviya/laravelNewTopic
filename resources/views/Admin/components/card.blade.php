<label class="{{ $class }}">{{$label}}</label>
<div class="form-group">
    <input type="{{ $type }}" id="{{ $id }}"  class="form-control" name="{{ $name }}" placeholder="{{ $placeholder }}" />
</div>
